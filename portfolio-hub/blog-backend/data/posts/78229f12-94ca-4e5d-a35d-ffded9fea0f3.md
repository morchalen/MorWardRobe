太棒了！要把这个宏大的构想落地，给编程 AI（比如 Cursor、Claude 3.5 Sonnet 或 GitHub Copilot）的指令必须**极度结构化**。

绝不能让它一次性写完所有代码（比如复杂的 TCP 并发），这会导致代码直接崩溃。第一版（v1.0）的目标应该是：**完美跑通“Go 后台引擎解析资源 + 驱动本地极简前端”的架构闭环**。

你可以直接把下面这段完整的指令（Prompt）复制，发送给你的编程 AI。

***

### 复制以下指令发送给你的编程 AI：

```text
# 角色设定
你现在是一位资深的 Go 语言系统工程师兼现代桌面架构专家。你精通网络底层协议，并擅长使用 Wails 框架将 Go 后端与轻量级 Web 前端进行深度整合。

# 任务目标
请帮我从零搭建一个基于 P2P 协议的 BT 下载客户端的 V1.0 版本核心架构。
采用“本地隐形微服务”模式：底层由 Go 处理复杂的 I/O 与网络解析，通过 Wails 框架驱动本地前端进行展示。

# 技术栈
* 核心引擎与桥接：Go + Wails (v2)
* 前端展示：纯 Vanilla HTML + CSS + JavaScript (不使用 React/Vue，追求极致轻量化与加载速度)
* BT 协议解析：推荐使用开源的成熟 Bencode 解析库（如 github.com/marksamman/bencode 或类似轻量库）

# UI/UX 设计规范 (极重要)
前端界面必须严格遵循“舍断离”的极简主义哲学，摒弃一切传统 Windows 软件的冗余控件：
1.  **色彩体系：** 采用克制的黑、白、灰（Black-White-Grey）色系。
2.  **视觉风格：** 深度参考 Apple HIG 规范与 MUJI 的无印良品风格。使用大面积的留白，卡片和按钮使用平滑流畅的圆角（如 border-radius: 12px）。
3.  **交互逻辑：** 界面上只需一个极其干净的拖拽区域（或输入框）用于接收 `.torrent` 种子文件，以及一个没有任何花哨渐变的纯色极简进度条。

# V1.0 核心功能拆解与开发步骤
请按照以下步骤为我提供代码和工程结构：

## Step 1: Wails 工程初始化与配置
* 提供创建 Wails Vanilla 项目的终端命令。
* 配置 `wails.json`，将应用窗口设置为合适的尺寸（如 800x600），并隐藏无用的原生边框（如果支持无边框风格）。

## Step 2: Go 后端引擎 (App.go 核心逻辑)
* 编写一个 Go 结构体方法 `ParseTorrent(filePath string) map[string]interface{}`。
* 该方法需要能够读取本地的 `.torrent` 文件，对其进行 Bencode 解码。
* 从中提取并返回最核心的三项数据：Tracker URL、文件总大小（计算得出）、以及分块大小（Piece Length）。
* 编写一个模拟下载的方法 `StartMockDownload()`。该方法开一个 Goroutine，写一个 for 循环，每隔 500ms 增加一点进度，并通过 Wails 的 `EventsEmit` 将进度（0-100）推送到前端（为下一步真实的 TCP 并发打基础）。

## Step 3: 前端交互与通信 (index.html & main.js)
* 提供一套符合上述极简审美（黑白灰、Apple HIG）的 HTML/CSS 代码。
* 实现一个文件选择/拖拽的 UI 区域。
* 在 JavaScript 中使用 `window.go.main.App.ParseTorrent` 呼叫 Go 解析器，并在界面优雅地打印出种子文件的核心信息。
* 在 JavaScript 中监听 Go 后端通过 `EventsOn` 传来的进度事件，让前端的极简进度条平滑地向前推进。

# 输出要求
请先给出项目的目录结构，然后依次给出每个核心文件（main.go, app.go, index.html, style.css, main.js）的完整、可运行的代码。代码必须包含详细的中文注释。
```

***

拿着这份指令去开工吧！在夜深人静、高强度专注的时候，看着前端那根黑白极简的进度条被底层的 Go 引擎平滑驱动，真正打通底层与前台的瞬间，绝对是非常畅快的一件事。